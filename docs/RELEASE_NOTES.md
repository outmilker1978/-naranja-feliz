# Naranja Feliz — Release Notes

## v0.8.0 (задеплоен 2026-09-04) — скорость по всем дашборд-страницам + чат на 1 запрос

### Чат на 1 запрос (#38 / Деплой 1)
- **Раньше:** чат открывался за 5–7 запросов (`/api/auth/me` + `/api/chat/teachers` + `/api/chat` + N подзапросов Supabase).
- **Теперь:** 1 вызов серверного `POST /api/chat/init` → RPC `get_chat_data` в одной транзакции (данные профиля, учителя, история, подписка). Fallback `legacyInit` на старой логике, если RPC недоступен — чат не ломается.
- Файлы: `src/app/api/chat/init/route.ts`, `src/app/(dashboard)/tools/chat/page.tsx`.

### Депликация `auth.getUser`/`profiles` на дашборде (Деплой 2)
- **Раньше:** каждая серверная страница + layout дёргали Supabase по 2–3 раза (`auth.getUser` ×3, `profiles` ×2), независимо дублируя друг друга.
- **Теперь:** общий `src/lib/auth-cache.ts` — `getServerClient()` и `getCurrentUser()` через `React.cache()`: **1 клиент и 1 `auth.getUser` + 1 `profiles` на HTTP-запрос**.
- Затронуты: layout дэшборда, курсы (список/курс/урок), настройки, учительская (учителя, проверка), инструмент чат. Страницы лоадатся быстрее (меньше сетевых запросов к Supabase).

### Не сделано: B7 (кэш публичных страниц) — отклонено технически
- Проверено: `export const revalidate` на публичных страницах **не включает ISR** — они остаются dynamic, т.к. `supabaseFetch` (server.ts) ходит обычным `fetch` без `next:{revalidate}`. Кэш контента учителя рискован («не показывать устаревшее»). Возврат только при росте нагрузки.

### Проверка
- `npx tsc --noEmit` — чисто. `npm run build` — успешно (все дашборд-страницы собираются).
- Браузерное регресс-тестирование до деплоя: `/`, `/login` (вход админа), `/courses`, курс, урок (5 типов блоков), `/tools/chat`, `/settings`, `/admin/teachers`, `/admin/submissions` — 0 ошибок.
- Деплой: `73e60ff` → ревизия `deploy-1788547720` (ACTIVE). `/api/chat/init` и `/api/stats` на проде отвечают 401 без авторизации (корректно).

---

## v0.7.3 (2026-09-04) — фиксы UX после деплоя B1

### Доступ к неопубликованным урокам (#1)
- **Админ/владелец курса** теперь открывают неопубликованный (черновик) урок — раньше редиректило обратно. Директор мог видеть урок в списке, но не открыть — исправлено.
- Список уроков в курсе больше **не показывает черновики обычным ученикам** (видны только автору и админу) — правка в RPC `get_course_page`.
- `page.tsx` урока: редирект на `/courses/{id}` для черновиков пропускает `isAdmin`.

### «Очистить ответы» — мгновенно (#2)
- Раньше: 2 клиентских запроса + полная перезагрузка страницы (урок с 13 блоками висел ~1 мин).
- Теперь: 1 SQL-функция `clear_lesson_answers(student_id, lesson_id)` — удаляет ответы + сбрасывает прогресс **в одной транзакции**, затем быстрый `router.refresh()` без перезагрузки.
- SQL в `supabase/rpc-aggregation.sql` (применён на проде).

### Картинки / аватары — быстрее и стабильнее (#3)
- **Единый компонент `<Avatar>`** (`src/components/avatar.tsx`): все аватары (шапка, настройки, чат, уведомления, учительская, проверка) идут через прокси `/api/storage` с маленьким размером (`?w=36&fm=webp`) вместо тяжёлого оригинала; если картинка не грузится — показывает **первую букву имени**. Убрана «разнобойность» (где-то грузилось, где-то нет).
- **Кэш в прокси `/api/storage`**: in-memory кэш обработанных картинок (10 мин, до 500 записей) — повторные запросы тех же URL идут из памяти без повторного sharp/fetch. Заголовок `X-Storage-Cache: hit/miss`.
- Картинки отдаются лёгкими (пр. аватар `w=36` = ~2–3 KB, hero `w=400` = ~24 KB) с `Cache-Control: immutable` на 24 часа — браузер повторно не скачивает.

### Статистика — всегда свежие цифры (#5)
- `/api/stats` переведён с кэша (`s-maxage=60`) на `Cache-Control: no-store` — «устаревшее» число пользователей (18 после удаления тестовых) больше не застревает; кнопка «Обновить» всегда даёт актуальное (5 пользователей).

### Инфраструктура / прочее
- **Удалены тестовые учётки** (16 шт.: 13 `@test.ru` + 3 безымянных) через GoTrue admin API. Осталось 5 реальных: `outmilk@yandex.ru` (админ), `outmilker@gmail.com` (владелец), `mrakobesochkabrel@gmail.com` (директор/учитель), `madhamster78@gmail.com` (ученик Пухляш), `test@test.com`.
- Деплой: `de0178d` → ревизия `deploy-1788544153` (ACTIVE, 200.23 МБ, 6 уязвимостей).

---

## v0.7.2 — готово к деплою

### Оптимизация N+1 → Postgres RPC (главный выигрыш по скорости)
Страницы больше **не опрашивают БД по 6–12 раз подряд** — сервер получает всё одним вызовом функции на Postgres (`SECURITY DEFINER`). 5 RPC-функций + 14 индексов в `supabase/rpc-aggregation.sql`:
- **Урок** (`get_lesson_page`): 12 → **1** запрос. Уроки с тяжёлыми блоками (Порядок/Групповой порядок/Память) открываются мгновенно (проверено: 13 блоков за ~0.2с одним вызовом).
- **Курс** (`get_course_page`): 6 → **1**.
- **Главная** (`get_home_data`): 10 → **1**.
- **Список курсов** (`get_course_list`): 9 → **1+1** (RPC + 1 доп. для прогресса).
- **Чат** (`get_chat_data`): подготовлен, подключение в следующих итерациях.
- Итог: **37 → 5** запросов по четырём главным страницам.

### Производительность / надёжность
- **Ускорение уроков (issue #34):** сборка всех сохранённых ответов ученика **одним** серверным запросом в `page.tsx` вместо **9** клиентских запросов к `block_submissions`. Урок 4 («Порядок», «Групповой порядок», «Игра в пары») грузится мгновенно.
- **Чат — автопрокрутка (issue #31):** после исправления скроллит вниз только при открытии чата и отправке своего сообщения; при фоновом обновлении (каждые 5с) позицию чтения вверх больше не сбрасывает.
- **Закалка соединений к Supabase (issue #35):** `Connection: close` на всех серверных клиентах + `/api/auth/me` (BFF) для клиентских страниц. Устранены случайные зависания 20–70с (в dev и на медленных сетях).
- **Инструменты (чат/карточки/словарь)** переведены с браузерного `supabase.auth.getUser()` на серверный `/api/auth/me` — открываются за ~1с, без браузерных зависаний.

### Проверено
- `tsc --noEmit` чист, `npm run build` проходит.
- Логин ~0.4с, главная ~2с, инструменты ~0.6–0.9с; 20/20 запросов без столов.

## v0.7.1 (2026-09-03) — Текущая

### Хотфикс: ускорение Hero-слайдшоу (ковер загрузки на главной)
- Слайды Hero переведены на **WebP** (`?fm=webp`, `?w=1280&q=65` вместо тяжёлого JPEG `?w=1600&q=72`) — передача в ~3-4 раза легче на медленных сетях.
- **Первые два слайда предзагружаются сразу при открытии страницы** (раньше первый фон ждал полный ресайз+передачу до первого показа — из-за этого Hero висел до ~60с). Теперь Hero появляется почти мгновенно.

### Выводы диагностики (проверено на проде)
- **Сжатие (gzip) JS и HTML уже работает** (проверено через `curl`, `Content-Encoding: gzip`). Страницы отдают сжатый контент (~250KB JS на чат).
- Сервер быстрый (HTML 200–700мс, картинки через прокси 0.5–0.9с).
- Остаточная тормознутость на первых заходах — сеть/провайдер до Yandex Cloud + непрогретый кэш; JS отдаётся с `immutable`, повторные визиты быстрые.
- nginx-прокси / предсжатие **не потребовались** — сжатие уже активное.

## v0.7.0 (2026-09-03)

### Мобильная оптимизация загрузки (issue #28)

**Главный фикс — «картинки не грузятся»:**
- Прокси `/api/storage` научился обрабатывать **оба** типа ссылок Supabase Storage — обычные (`/object/public/...`) и **подписанные** (`/object/sign/...?token=...`). Раньше подписанные уходили напрямую на `supabase.co` и не доезжали до браузера (провайдер обрывал соединение). Теперь все картинки идут через прокси.

**Оптимизация публичных страниц:**
- Публичные страницы переведены на компонент `StorageImage` (next/image): responsive `srcset`, **ленивая загрузка**, защита от «прыжков» вёрстки (`fill`).
- Сжатие картинок на лету через Sharp в прокси: `?w=` (ширина), `?q=` (качество), `?fm=` (формат), авто-поворот по EXIF, отдача WebP/AVIF по Accept браузера.
- **Кэш** `Cache-Control: max-age=86400, immutable` — повторные загрузки мгновенные.
- Мигрированы: главная, `/catalog`, `/content/[id]`, `/reviews`, `/about`, `/teachers`, `/teachers/[id]`, карусель контента, слайдшоу, CTA.
- Тяжёлые клиентские библиотеки (редактор/ProseMirror, recharts-графики) подтверждены **вне публичного пути** — на лендинг не подгружаются.

**Новые загрузки:**
- `/api/upload-file` теперь возвращает **public URL** вместо подписанного (надёжнее для будущих картинок). Существующий контент учителя **не изменён** — старые картинки работают через прокси как раньше, но быстрее.

**Прочее:**
- `TwemojiGlobal` — пропуск уже обработанных поддеревьев (меньше нагрузки при перерисовке).
- В dev-режиме для удобной проверки вёрстки добавлена заглушка-плейсхолдер вместо реального файла (на прод не влияет — там только `NODE_ENV=production`).

### Исправлено
- Навигация и загрузка публичных страниц — стабильны (ранее подписанные картинки «вешали» страницу до таймаута ~70с).
- Исправлена поломка переходов в dev при нескольких экземплярах сервера (очистка `.next/dev`).

## v0.6.0 (2026-08-05)

### Добавлено
- **Игра в пары (memory)** — новый тип блока урока: 12/16/20 карточек (6/8/10 пар). Рандомная раскладка, учёт шагов, авто-прогресс урока.
- **Эмодзи-пикер** в Tiptap-редакторе (8 групп смайлов) + цветной шрифт Twemoji Mozilla (`public/fonts.css`, unicode-range только эмодзи).
- **Кредитная система доступа** — учитель выдаёт доступ подарком или в кредит; оплата сначала гасит долг; история операций (`subscription_credit_history`).
- **Директор школы** — админ назначает директора, запросы продления/доступа идут директору (иначе первому/всем учителям).
- **Уведомления о подписке (сайт + email)** — за 5 дней / за 1 день / «закончилась»:
  - Мгновенная проверка при входе студента (`/api/subscription/check` + `SubscriptionCheckOnLogin`).
  - Cron endpoint `/api/cron/subscription-expiry` + GitHub Actions schedule ежедневно (`cron-subscription.yml`).
  - Email через SMTP Яндекс (nodemailer), retry через таблицу `subscription_email_log`, ошибки логируются.
  - Сайт-уведомление → `/settings`, email → `/pricing`.

### Исправлено
- **FillBlank «Сохранить»** — активируется после «Проверить» при введённых значениях (не зависит от правильности).
- **Кнопка «Отметить урок пройденным»** — восстановлена, авто-прогресс включает все типы блоков (в т.ч. `group_drag` и `memory`).
- **Модалка «Доступ к курсам»** — устранено мерцание при авто-открытии (очистка URL-параметров).
- **Admin доступ к курсам учителя** — обход проверки для админа.
- **Мусор** — удалены dev-логи (добавлены в `.gitignore`), временный диагностический скрипт.

## v0.5.1 (2026-07-28)

### Добавлено
- **GroupDragBlock** — полноценный рендерер с drag-and-drop: слова анонимные, перемешанные. Drag пул→слот, слот→слот, слот→пул. Клик-фоллбек. Сохранение попыток + восстановление ответа при перезагрузке.
- **DragOrderBlock / ImagePickBlock** — восстановление ответа при перезагрузке страницы.

### Исправлено
- **OrangeDivider** — лишняя закрывающая скобка в `renderHTML` (Array bracket mismatch, билд падал).
- **TiptapEditor** — кнопка Bold была активна при пустом тексте в первом блоке. Добавлен автосброс маркера bold при инициализации.
- **textarea ввода слов группы** — Enter/пробелы не обрезали вводимый текст (убран `filter(Boolean)` из onChange).

## v0.5.0 (2026-07-19)

### Добавлено
- **Per-course доступ** — студент запрашивает доступ к курсу, учитель выдаёт на срок, студент сразу входит без кнопки «Записаться».
- **Роуты доступа:** POST /api/course-access/request, POST /api/course-access/grant, GET /api/course-access/check, GET /api/course-access/student-courses, POST /api/course-access/revoke.
- **Режим курса «По запросу»** — опция per_course в админке при создании/редактировании курса.
- **Авто-enrollment** — при выдаче доступа автоматически создаётся enrollment.
- **Умная кнопка на странице курсов** — при 403 кнопка «Записаться» меняется на «Запросить доступ у учителя».
- **Модалка учителя** — отображение выданных доступов, кнопка «Отозвать».
- **Уведомления с контекстом** — клик открывает учительскую с модалкой студента и предвыбранным курсом.
- **orce-dynamic** на страницах курсов и учительской (данные всегда свежие).
- **proxyImgUrl()** теперь используется во всех местах (главная, каталог, курсы, админка).

### Исправлено
- **canAccess** на странице курса — больше не пропускает per_course без доступа.
- **Enroll API** — проверяет per_course доступ (раньше только subscription).
- **EnrollButton** — alert() заменён на инлайн-сообщение.
- **Lesson guard** — проверяет course_access ДО enrollment.
- **Модалка** — без дергания, с кнопкой ×, клик по фону, авто-закрытие.
- **Уведомления запроса** — шлются и учителям, и админам.
- **Разделитель в редакторе** — переписан на ReactNodeViewRenderer: в редакторе рендерится React-компонент (SVG не вырезается браузером), в сохранённом HTML — полный SVG.
- **Выравнивание картинок** — `TextAlign.configure({ types: ["image"] })` + CSS `img[style*="text-align"] { display:block; margin:auto }` в TextBlockRenderer. В редакторе выравнивание через `ResizableImageComponent`, в уроке/предпросмотре — через margin.
- **Учитель в режиме ученик** — владелец курса пропускает `check_course_access` (owner bypass в 3 местах).

## v0.4.4 (2026-07-12)

### Добавлено
- **Прокси-роут `/api/storage/[...path]`** — сервер-серверный fetch до Supabase Storage с Sharp-сжатием (1920px, JPEG q80). Решает ERR_CONNECTION_RESET при загрузке фото через провайдера.
- **Прокси для всех изображений** — все фото на главной, отзывах, о школе, контенте идут через `/api/storage/...` (Sharp-сжатие).
- **Серверная регистрация** — `POST /api/auth/signup` через `admin.createUser()` с auto-confirm (без письма). Создание профиля в `profiles`.
- **Серверный вход** — `POST /api/auth/login` через `signInWithPassword()` с cookie.
- **Сброс пароля** — `/forgot-password`, `/reset-password`, API `/api/auth/forgot-password`, `/api/auth/update-password`. Ссылка «Забыли пароль?» на странице входа.
- **Пакетное сжатие фото в Storage** — скрипт `scripts/compress-storage.mjs` прошёлся по всем bucket'ам, сжал >1MB файлы. Экономия: 5-10MB → 200-500KB.

### Исправлено
- **Auth callback** — использует `NEXT_PUBLIC_SITE_URL` вместо `request.url` (на Yandex Cloud request.url = http://0.0.0.0:8080).
- **Health check в деплое** — стучится на прямой URL контейнера, не на домен (SSL не сломан).
- **TypeScript** — `Buffer → Uint8Array` в proxy роуте (Next 16 не принимал Buffer как BodyInit).
- **Type error в сборке** — сброс `.next` кэша убрал ложную ошибку в сгенерированном validator.ts.

## v0.4.1 (2026-07-11)

### Добавлено
- **Множественные версии hero/cta** — можно создавать черновики, активна только опубликованная
- **Статусная индикация** в списке контента — зелёный «Активен»/серый «Черновик», полупрозрачность для неактивных
- **lucide-иконки для всех категорий** секций (hero→Sparkles, features→Sparkles, about→Heart, testimonials→Star, faq→HelpCircle) вместо двухбуквенных кодов
- **💾→HardDrive, 📁→Folder** для Яндекс.Диск/Google Диск в рендерере блоков
- **Полная архитектурная документация** — `docs/ARCHITECTURE.md`
- **CI/CD через GitHub Actions** — `.github/workflows/deploy.yml` (авто-деплой при пуше в main)

### Исправлено
- Портал отображает 4 карточки по умолчанию (включая «Проверку») — соответствует редактору
- **deploy-016** (11.07.2026): первый деплой с Yandex Cloud переменными окружения
- Документация `DEVELOPER_GUIDE.md` актуализирована под Yandex Cloud (было Vercel)

## v0.4.0 (2026-07-10)

### Добавлено
- **Портал школы** — главная страница собирается из секций (Hero, Features, About, Testimonials, FAQ, CTA) с сортировкой через `sort_order`. Блоки новостей, статей, рекламы — выезжающие карусели.
- **Админ-панель контента** (`/admin/content`) — иерархические блоки с перетаскиванием (▲▼). HERO и CTA заблокированы (первый/последний). Коллекции (Новости, Статьи, Реклама) — раскрывающиеся с внутренней сортировкой.
- **Фиксированная навигация на всех content-страницах** — панель прилипает под хедером при скролле:
  - `/content/[id]` — `← Все новости/статьи` + `Следующая новость/статья →`
  - `/courses/[courseId]` — `← Все курсы` + `Первый урок/Продолжить →`
  - `/courses/[courseId]/[lessonId]` — `← Название курса` + `Следующий урок →`
- **Сжатие изображений** при загрузке — Sharp (JPEG mozjpeg quality 82, PNG → WebP если >500KB, ресайз >1920px). При ошибке sharp — fallback на оригинал.
- **Signed URL (1 год)** для всех загружаемых файлов — гарантия доступа даже если bucket не публичный.
- **4-я карточка быстрого доступа** в Hero — «Проверка» (PenLine, для учителей) в дефолтах редактора.

### Исправлено
- **Загрузка фото**: если Sharp не может обработать файл — грузится оригинал (фото не пропадают).
- **Сохранение учителя**: PUT `/api/content/[id]` пропускает `category: null` (ошибка 500).
- **CTA sort_order**: при создании CTA-секции через POST присваивается `max + 1`.
- **Портал**: секции рендерятся в порядке `sort_order` (hero → ... → cta).
- **Страница отзывов**: текст ярко-белый (`text-white/95`) на тёмном фоне.
- **Удалён дубль `position: sticky`** из site-header (управляется только внешним wrapper'ом).

### Изменено
- **Sub-navigation**: `position: fixed; top: 73px` вместо sticky (на всех content-страницах).
- **Admin content list**: полный редизайн — группировка в блоки, locked HERO/CTA, expandable коллекции.
- **upload-file**: добавлена компрессия через Sharp с fallback при ошибке.
- **Hero editor**: заголовки разделов «Статистика» и «Карточки быстрого доступа» — жирный шрифт.

### Архитектурные решения
- Все изображения при загрузке проходят через Sharp (оптимизация размера) с try-catch fallback.
- Signed URL (1 год) — единый механизм доступа к файлам Storage.

---

## v0.3.0 (2026-07-05)

## v0.2.0 (2026-07-01)

### Добавлено
- Блоки: текст, изображение, видео, fill_blank, choice, open_question, audio_answer, drag_order, image_pick.
- Админ-панель: управление курсами, уроками, блоками, учителями.
- Ролевая система: ученик / учитель (переключение через cookie).
- Система проверки: учитель просматривает ответы, оставляет комментарий, отмечает «проверено».
- Чат под каждым ответом (SubmissionThread) — студент и учитель могут переписываться.
- Уведомления: колокольчик с бейджем, страница `/notifications`.
- Прогресс: дольки апельсина на карточках курса и в списке уроков.
- Автозавершение урока при выполнении всех интерактивных блоков.
- Геймификация: уровни (Новичок → Профессор) в зависимости от числа пройденных уроков.
- Страница настроек: аватар, имя, email, пароль, роль.
- Supabase Storage: bucket `lesson-files`, загрузка аудио, изображений.
- API: `/api/upload-audio`, `/api/comments`, `/api/review-submission`, `/api/assign-teacher`, `/api/enroll`, `/api/notifications`, `/api/stats`.
