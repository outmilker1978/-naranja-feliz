# Naranja Feliz — План Развития

## Легенда
- 🟢 готово — работает сейчас
- 🟡 в работе / ближайшие дни
- 🟠 на неделю
- 🔴 на месяц и далее / отложено
- 🔒 требует доступа к GitHub/Supabase (заблокировано 2FA) — сейчас сделать нельзя
- ✅ завершено (см. RELEASE_NOTES)

---

## 0. Оптимизация к продаже (этапы B/C/D + Android) — актуальный план от 04.09.2026

**Цель:** стабильно/быстро/доступно → готовность к платящим пользователям + мобильному Android.
**Доступ:** GitHub + Supabase **восстановлены** (2FA решён) — деплой и SQL-миграции работают (см. v0.7.2/v0.7.3).

| № | Задача | Статус | Комментарий |
|---|--------|--------|-------------|
| B1 | Убрать N+1 → агрегация через Postgres **RPC** (`get_home_data`, `get_course_page`, `get_lesson_page`, `get_chat_data`) | ✅ готово | `supabase/rpc-aggregation.sql` применён. Урок/курс/главная/список курсов на 1 вызов (37→5). Чат подключён к `get_chat_data` (код готов, ждёт деплоя). v0.7.2 (`302238e`) + v0.7.3 (`de0178d`) на проде |
| B2 | HTTP-клиент: undici Agent вместо `Connection: close` | ✅ решено | `undici` недоступен без новой зависимости; ставить рискованно → оставлен проверенный `Connection: close` + таймаут/ретрай. Вернуться при росте нагрузки |
| B3 | Таймауты + ретраи на внешних вызовах (Supabase; GET — 1 ретрай) | ✅ готово | `server.ts` `supabaseFetch`: таймаут 12с + ретрай только для GET (не дублирует изменения). 0/6 столов |
| B4 | Логирование ошибок на прод (видно падения) | ✅ готово | `src/lib/logger.ts` + ошибки Supabase в лог с маркером `[nf:]`, видно в логах контейнера |
| B5 | Rate-limit на ключевых API + роут-гварды | ✅ готово | Лёгкий in-memory лимит: вход (15/мин), перевод (90/мин); submit-answer уже с auth-guard |
| B6 | Не светить ключи в docs | ✅ готово | Анон-ключ замаскирован в ARCHITECTURE.md; реальных секретов в доках нет |
| B7 | Кэш публичного контента | 🔴 отложено | Проверено технически: ISR (`revalidate`) не включается — страница остаётся dynamic из-за кастомного `supabaseFetch` (без `next:{revalidate}`). Кэш контента учителя = риск устаревания («не трогать контент»). Польза при малом трафике низка. Возвращаться только при росте нагрузки |
| C1 | Supabase Free Tier: тёплая БД + мониторинг лимитов | 🔴 | Требует доступа (есть); при росте — БД с always-on |
| C2 | Yandex container: cold start / concurrency | 🔴 | Настройка в Yandex Cloud |
| D1 | **PWA** (manifest + service worker + offline-кэш) | ✅ готово | `public/manifest.webmanifest`, `public/sw.js`, регистрация в проде; база для Android |
| D2 | Android-обёртка (Capacitor/TWA) поверх Next-API (BFF) | 🟠 | После стабилизации; мобилка ходит в `/api/*`, не в Supabase |
| D3 | API-first: версионные `/api/*`, минимальные ответы | 🔴 отложено | Широкий рефактор ответов — риск сломать фронтенд; делать после B1/доступа единым заходом |

**Уже сделано (завершено):**
- ✅ B1 — N+1 → Postgres RPC, 4 страницы на 1 вызов (`supabase/rpc-aggregation.sql`), задеплоено v0.7.2.
- ✅ #34 — централизация `block_submissions` одним серверным запросом.
- ✅ #31 — автопрокрутка чата не сбрасывает чтение.
- ✅ #35 — закалка Supabase-соединений + `/api/auth/me` (BFF) для инструментов.
- ✅ v0.7.3 (`de0178d`) — админ открывает черновики уроков, «Очистить ответы» через 1 RPC, единый `<Avatar>` + кэш `/api/storage`, `no-store` для `/api/stats`, удалены 16 тестовых учёток.

**План на следующие сессии (зафиксировано 04.09.2026):** деплои по одному на задачу. Почему по одному: Деплой 2 трогает ВСЕ страницы, Деплой 4 — кэш контента учителя (нельзя показывать устаревшее). Если что-то сломалось — сразу видно из-за чего, ничто не смешивается.

| Деплой | Задача | Что входит (состав) | Как | Результат | Риск | Оценка времени |
|--------|--------|---------------------|-----|-----------|------|----------------|
| 1 | **Чат на 1 запрос** (get_chat_data) | `tools/chat/page.tsx` — вызов серверной функции вместо 5–7 запросов (функция уже готова в SQL) + fallback на старый путь | Заменяем клиентские вызовы одним серверным (`/api/chat/init`) | Чат открывается/обновляется за ~1 сек вместо нескольких | Низкий (одна страница) | ~1 час |
| 2 | **Убрать дубли в layout** (3×`auth.getUser` → 1, 2×`profiles` → 1) | Общий каркас дэшборда (layout) + все серверные страницы; `src/lib/auth-cache.ts` (`getServerClient`/`getCurrentUser` через `React.cache`) | 1 вызов на запрос, результат прокидывается вниз | Все страницы грузятся быстрее (меньше запросов к Supabase) | Средний (затрагивает все страницы — тестить вход и каждую вкладку) | ~2 часа |
| 3 | **#37 Управление пользователями** в учительской | Новый раздел: список пользователей (поиск), кнопка «Удалить» с подтверждением + серверный API удаления (GoTrue admin + profiles) | UI + серверная функция | Директор/админ удаляет лишние учётки сам, без разработчика (раньше чистили 16 тестовых вручную) | Средний (новая фича, тест ролей: директор/админ/учитель) | ~3 часа |
| — | **B7 Кэш публичных страниц** | ❌ невыполнимо в текущем виде | ISR не включается: `supabaseFetch` не передаёт `next:{revalidate}` → страницы dynamic. Кэш контента учителя = риск устаревания | — | — | — |

**Статус на 04.09.2026 (код готов, НЕ задеплоен):** ✅ Деплой 1 (чат) и ✅ Деплой 2 (дубли layout): `tsc --noEmit` чисто, `npm run build` успешен. Деплой 4 (B7) отклонён технически (см. таблицу). Деплой 3 (#37) — отложен пользователем на потом.

**Порядок оставшихся:** 1 → 2 (можно вместе, оба протестированы) → 3 (когда пользователь даст добро). Каждый деплой = отдельный короткий чат (правило «одна задача = один короткий чат»). Первый шаг новой сессии: «продолжи по docs/ROADMAP §0» — план восстановится из файла.

**После Деплоя 2:** C1/C2 (аренда Supabase/Yandex при росте) → D2 (Android) → #26 ЮKassa (ждёт ключей пользователя из личного кабинета).

---

## 1. Производственная база (учитель может делать уроки)
**Цель:** учитель загружает материалы, не отвлекаясь на баги

| Задача | Статус |
|--------|--------|
| 🟢 Все типы блоков работают | 🟢 |
| 🟢 DragOrder — drag-and-drop + клик | 🟢 |
| 🟢 ImagePick — редактор с загрузкой | 🟢 |
| 🟢 FillBlank, Choice, Image/Video | 🟢 |
| 🟢 VideoAnswer — запись с камеры | 🟢 |
| 🟢 Уведомления от DragOrder и ImagePick | 🟢 |
| 🟢 Учебник (курс → уроки → блоки) | 🟢 |
| 🟢 Преподавательский состав | 🟢 |

---

## 2. Подписка и доступ
**Цель:** оплатил — видишь курсы. Срок истёк — только ЛК и инструменты.

| Задача | Статус |
|--------|--------|
| 🟡 Поле `subscription_until` в `profiles` | 🔴 |
| 🟡 Guard проверки подписки на уроках | 🔴 |
| 🟡 Страница «Купить подписку» | 🔴 |
| 🟡 Уведомление об истечении подписки | 🔴 |
| 🟠 Интеграция с платёжным шлюзом (ЮKassa / Т-Банк) | 🔴 |

---

## 3. Личный кабинет студента / Инструментарий
**Цель:** профиль, прогресс, уровень языка, инструменты (чат, тесты, словарь) — доступны всегда, даже без подписки

| Задача | Статус |
|--------|--------|
| 🟢 Аватар, имя, email, пароль | 🟢 |
| 🟢 Прогресс (🍊 дольки) + уровни | 🟢 |
| 🟡 Личный чат с учителем (polling 5с, потом Realtime) | 🟢 |
| 🟡 Уровень языка (A1-C2) + подтверждение учителем | 🟢 |
| 🟠 Тестирование (инструмент) | 🔴 |
| 🟠 Словарь (инструмент) | 🔴 |

---

## 4. Контроль и отчётность для учителя
**Цель:** учитель видит успеваемость, проверяет, анализирует

| Задача | Статус |
|--------|--------|
| 🟢 Проверка ответов (Принять + комментарий) | 🟢 |
| 🟢 Чат под ответом (SubmissionThread) | 🟢 |
| 🟢 Уведомления (колокольчик + страница) | 🟢 |
| 🟢 Аватар отправителя в уведомлениях | 🟢 |
| 🟠 Статистика: бублики по уровням курсов и студентов | 🔴 |
| 🟠 Фильтры в «Проверка» | 🟢 |
| 🟠 История проверок / журнал | 🔴 |

---

## 5. Дизайн и портал
**Цель:** сайт выглядит как школа, а не как конструктор

| Задача | Статус |
|--------|--------|
| 🟢 Главная страница (портал школы) — Hero, Features, About, Testimonials, FAQ, CTA | 🟢 |
| 🟢 Новостная лента / блог (карусель на главной + страница новости) | 🟢 |
| 🟢 Рекламные блоки (карусель баннеров) | 🟢 |
| 🟢 Фиксированная навигация на всех content-страницах | 🟢 |
| 🟢 Админ-панель контента (иерархические блоки + реордеринг) | 🟢 |
| 🟠 Лендинг с тарифами | 🟢 |

---

## 6. Деплой и инфраструктура
**Цель:** сайт работает 24/7, данные не теряются

| Задача | Статус |
|--------|--------|
| 🟢 Локальный запуск (npm run dev) | 🟢 |
| 🟡 Деплой на Vercel | 🔴 |
| 🟡 CI/CD (авто-деплой при пуше в main) | 🔴 |
| 🟡 Бэкапы БД (Supabase) | 🟢 |
| 🟡 Мониторинг ошибок (Sentry) | 🔴 |

---

## 7. Марафоны

**Идея:** Новый тип учебного материала на уровне курсов — «Марафон».

- По структуре как курс: внутри уроки
- Уроки публикуются по расписанию (от даты X до даты Y)
- Доступ — разовый платёж (не подписка), индивидуальный
- После оплаты ученик получает доступ к марафону и следит за расписанием

### Что нужно сделать:
| Задача | Статус |
|--------|--------|
| Тип курса `marathon` (или новая таблица) | 🔴 |
| Поля `start_date`, `end_date`, `lesson_publish_schedule` | 🔴 |
| Разовый платёж через ЮKassa (отдельный от подписки) | 🔴 |
| Логика доступа: `check_marathon_access(user_id, marathon_id)` | 🔴 |
| UI: создание марафона учителем | 🔴 |
| UI: календарь/расписание для ученика | 🔴 |

---

## 8. Упрощение контента

Категории (`category`) у типов `news`, `article`, `ad` не влияют на отображение:
- **news**: announcement / event / achievement — сырой текст на странице, бесполезно
- **article**: grammar / culture / tips — то же самое
- **ad**: banner / promo — не используется вообще

**Надо:** убрать поле `category` из редактора для этих типов (оставить только для `page_section`), очистить старые записи.

### page_section: дубль «Заголовок» и «Заголовок секции»
Общее поле `title` (столбец) и поле `content.title` (в JSON) дублируются. При рендере на главной используется только `content.title`.

**Надо:** убрать общий `title` из формы для `page_section`, оставить только поля внутри секции.

### Реклама: дубль полей картинки
У `ad` два поля для одного изображения: «Обложка» (общая) и «Изображение (URL)» (в блоке баннера). При рендере `cover_image || content.image`. Одно лишнее.

**Надо:** оставить одно поле — загрузка файла (с ПК) + URL. Убрать дубль.

### Во все поля с URL картинки — добавить загрузку с ПК
Сейчас только «Обложка» умеет загружать файл с компьютера. Остальные поля (реклама → изображение, о школе → фото, отзывы → аватар) — только URL. Не у всех есть ссылка на картинку.

**Надо:** вынести загрузку файла в переиспользуемый компонент и воткнуть во все поля изображений.

---

## 9. Маркетизация (дальняя перспектива)

| Задача | Статус |
|--------|--------|
| 🔴 Яндекс.Директ / контекстная реклама | ❌ |
| 🔴 Инфлюенсеры: рекламные места в кабинете | ❌ |
| 🔴 KPI-скрипты действий пользователя | ❌ |
| 🔴 Партнёрская программа (рефералы) | ❌ |

---

## 10. Навигация в лонгридах

Стрелки «Предыдущая / Следующая» статья вверху страницы `/content/[id]` — фиксированная панель под хедером.

| Задача | Статус |
|--------|--------|
| 🟢 Фиксированная навигация на `/content/[id]`, `/courses/[id]`, `/courses/[id]/[lesson]` | 🟢 |

---

## Приоритет сегодня (установлены 10.07.2026)
1. 🟢 **Портал** — главная + новости + статьи + админка контента
2. 🟢 **Фиксированная навигация** — на всех content-страницах
3. 🟢 **Сжатие изображений** — Sharp с fallback
4. 🟡 **Деплой на Vercel** — сайт в продакшн
5. 🟡 **Подписка** — subscription_until + guard уроков + страница
6. 🟡 **Личный чат** (текст + файлы, polling 5с → Realtime при деплое)

После этого: дизайн под бренд, статистика, история проверок, маркетизация.
