# Issues & Changelog — Naranja Feliz

## Формат

```
№ | Проблема / Фича | Анализ / Решение | Статус
```

Статусы: `бэклог` → `в работе` → `готово` → `проверено` → `релиз`

---

## Текущие

| № | Проблема | Анализ | Статус |
|---|---|---|---|
| #1 | /about — иконка «читать далее» рендерится как текст | `page.tsx:285,373` — `button_icon` вставлялся в `<span>` как строка. Переписал на `ICON_MAP` — теперь рисует Lucide-компонент, если имя совпадает | проверено |
| #2 | HERO статистика: 1 курс, 10 уроков (неверно) | `page.tsx:174` — всегда берёт `courseCount`/`lessonCount` из БД, CMS-статы только c `slice(2)`. `content-editor.tsx:114` — убрал хардкод "10 уроков" | проверено |
| #3 | Статистика не доступна учителям | API stats уже пускает teacher, layout не блокирует, tools-panel показывает ссылку. Работает без изменений | проверено |
| #4 | Админы видны ученикам в чате как учителя | `chat/teachers/route.ts:26` — `.in("role",[teacher,admin])` → `.eq("role",teacher)`. Ученик видит только учителей | проверено |
| #9 | FillBlankBlock — курсор сбрасывается при вводе символа (regression) | `dangerouslySetInnerHTML` пересоздаёт DOM на каждый рендер. **Фикс:** initHTML через `useEffect` + `ref`, без `dangerouslySetInnerHTML` в JSX | проверено |
| #10 | GroupDragBlock — форма редактирования не открывается после добавления | `addBlock` вызывал `router.refresh()` который сбрасывал `editingIndex`. **Фикс:** убран `router.refresh()` из `addBlock` | проверено |
| #11 | AttemptsDots — переделать: вертикально, внизу, с текстовым комментарием | AttemptsDots переписан: точки вертикальные (5px), отображаются рядом с кнопкой, показывают текст «Неверно, осталось N попыток» / «Верно!» / «Попытки исчерпаны» | проверено |
| #13 | GroupDragBlock — написать рендерер с drag-and-drop | Слова анонимные, перемешанные. Drag пул→слот, слот→слот, слот→пул. Клик-фоллбек. Сохранение ответа + попытки + восстановление при перезагрузке | проверено |
| #14 | DragOrderBlock / ImagePickBlock — восстановление ответа при перезагрузке | Загрузка saved-ответа из block_submissions, восстановление позиций/выбора | проверено |
| #15 | OrangeDivider — renderHTML с лишней скобкой | Лишний `],` в return-массиве renderHTML. Убран, билд проходит | проверено |
| #16 | TiptapEditor — bold активен при пустом тексте | Добавлен useEffect: сброс bold-маркера, если в HTML нет `<strong>/<b>` | проверено |
| #17 | textarea ввода слов группы — Enter/пробел режут текст | Убран `filter(Boolean)` из onChange. Фильтрация только в buildContent | проверено |
| #18 | Большие фото не загружаются (413 Payload Too Large) | Yandex Cloud Gateway не пропускает >10MB. Фикс: добавил клиентскую компрессию (canvas → resize 1920px → JPEG 80) перед отправкой | готово |
| #19 | Админ не видит курс учителя | RPC `check_course_access` не имел проверки `role='admin'`. Обновлён в Supabase. + Добавлен `isAdmin` bypass на страницы курса, урока и списка | готово |
| #20 | Пропал функционал «отметить урок пройденным» | `CompleteLessonButton` и `LessonProgressTracker` импортированы в `[lessonId]/page.tsx:5-6`, но НЕ отрендерены в JSX (потерялись в рефакторинге v0.5.0 `df65c8f`). + `AutoCompleteLesson:26` не включает `group_drag`. **Проверка кода:** кнопка уже отрендерена (`page.tsx:133`), авто-прогресс включает `group_drag` и `memory`. Работает | готово |
| #21 | Новая фича: Игра в пары (memory/matching) | Тип блока memory: 12/16/20 карточек, рандомная раскладка, шаги, авто-прогресс. Кнопки размера с форматом. Шрифт Twemoji ограничен unicode-range. SQL выполнен. Задеплоено v0.6.0 | релиз |
| #22 | Новая фича: Эмодзи-пикер в редакторе | 8 групп смайлов, вставка в место курсора, не закрывается после вставки. Цветной шрифт Twemoji Mozilla (public/fonts.css). Задеплоено v0.6.0 | релиз |
| #23 | Уведомления о подписке не работали (сайт + email) | Мгновенно при входе + cron ежедневно. SMTP Яндекс (пароль приложения). subscription_email_log для retry. Дедупликация. Задеплоено v0.6.0 | релиз |
| #24 | Новая фича: Кредитная система доступа | Подарок/кредит/списание/закрытие. Оплата гасит долг. История операций. UI учителя и ученика. Задеплоено v0.6.0 | релиз |
| #25 | Новая фича: Директор школы | is_director + /api/set-director + UI. Запросы продления/доступа → директору, иначе учителям. Задеплоено v0.6.0 | релиз |
| #26 | ЮKassa не настроена | `YOO_KASSA_SHOP_ID` и `YOO_KASSA_SECRET_KEY` пустые в .env.local — оплата упадёт. Нужны ключи из личного кабинета ЮKassa. Отложено пользователем | бэклог |
| #27 | Модалка «Доступ к курсам» дёргается/мелькает | dismissed фиксируется, URL-параметры очищаются. Задеплоено v0.6.0 | релиз |
| #28 | Сайт очень медленно грузится на мобильном интернете | Публичные страницы мигрированы на `StorageImage` (next/image → прокси `/api/storage` с `?w/?q/?fm`, compression sharp, Accept-negotiation, ленивая загрузка, защита от layout-shift). Фикс корня «картинки не грузятся»: прокси теперь умеет **signed URL** (`/object/sign/...?token=`), `upload-file` возвращает public URL — браузер не ходит напрямую на supabase.co. Тяжёлые чанки (editor/ProseMirror 532KB, recharts 325KB) подтверждены **вне публичного пути** (route-splitting). TwemojiGlobal — пропуск уже обработанных поддеревьев. Hero-слайдшоу: WebP (`?w=1280&q=65&fm=webp`) + предзагрузка первых слайдов (раньше Hero висел до ~60с). Диагностика: gzip JS/HTML уже работает (curl), сервер быстрый, остаточная медлительность — сеть/холодный кэш. v0.7.0 (`02478ae`) + v0.7.1 (Hero hotfix) | релиз |
| #29 | FillBlank: «Проверить» не активирует сохранение/прогресс | isDisabled = saved || !hasAnyValue. Кнопка активна при введённых значениях. Задеплоено v0.6.0 | релиз |
| #30 | Модалка «Доступ к курсам» — редкое мерцание при выносе мыши за её пределы | После фикса #27 основное мерцание ушло. Остаток: если вывести курсор за границы модалки (на затемнение), изредка она мигает. Внутри модалки при наведении на поля мерцания нет | бэклог |
| #31 | Чат — автопрокрутка вниз к полю ввода каждые 1-2 сек (сброс прокрутки вверх) | `tools/chat/page.tsx:64` — `useEffect(..., [messages])` вызывал `bottomRef.scrollIntoView()` при КАЖДОМ обновлении сообщений (поллинг 5с отматывал вверх). **Фикс:** скролл только при открытии чата/первой загрузке и отправке своего сообщения (`pendingScrollRef`). Задеплоено v0.7.2 | релиз |
| #32 | Стратегическая оптимизация платформы для запуска к продаже + подготовки к Android (этапы B/C/D) | **Цель:** стабильно/быстро/доступно, подготовка к реальным платящим пользователям и мобильной версии. **B (производительность):** ✅ B1 — N+1 устранён через Postgres RPC (`supabase/rpc-aggregation.sql`): главная/курс/урок/список курсов на 1 вызов (37→5) + чат на `get_chat_data` (задеплоено v0.8.0); ✅ Dепликация layout (`src/lib/auth-cache.ts`, 1×`getUser`+1×`profiles` на запрос — задеплоено v0.8.0); ✅ B3 timeout+retry; ✅ B4 логирование; ✅ B5 rate-limit; ✅ B6 секреты; 🔴 B7 кэш публичного контента (отклонено технически: ISR не включается с кастомным `supabaseFetch`; риск устаревания контента учителя). **C (надёжность):** 🔴 C1/C2 (аренда Yandex/Supabase). **D (Android prep):** ✅ D1 PWA; 🟠 D2 Android-обёртка. | в работе |
| #33 | Проверка текущего пуша — собрался ли на проде | Обложки/блоки/чат задеплоены (`300a397..de0178d`), активная ревизия `deploy-1788544153`. Оформлено в RELEASE_NOTES v0.7.2/v0.7.3 | проверено |
| #34 | Централизация запросов `block_submissions` на уроке — убрать N+1 медленных блоков | Реализовано: один серверный запрос `.in("lesson_block_id", blockIds)` + `savedByBlock` прокинут во все 9 блоков. Задеплоено v0.7.2 | релиз |
| #35 | Локальный dev тормозит «десятками секунд» — логин висит | Фикс `supabaseFetch` (`Connection: close`) применён ко всем серверным клиентам + `/api/auth/me` (BFF) для инструментов. Задеплоено v0.7.2 | релиз |
| #36 | Оптимизация N+1 → Postgres RPC (этап B1) | `rpc-aggregation.sql` — 5 функций + 14 индексов, применены в Supabase, 4 страницы на 1 вызов (37→5). Дополнено `clear_lesson_answers` (очистка ответов, v0.7.3). Задеплоено v0.7.2/v0.7.3 | релиз |
| #37 | Инструмент управления пользователями для директора/админа (удаление неактуальных аккаунтов) | Новая фича: раздел/карточки в учительской, чтобы удалять аккаунты из БД (не вручную через API). Пользователь сам предложил после ручной чистки 16 тестовых учёток. Удаление через GoTrue admin API + profiles. UI: список, поиск, подтверждение | бэклог (отложен пользователем — приоритет у скорости сайта) |
| #38 | Чат на 1 запрос + дедупликация layout (скорость дашборда) | См. RELEASE_NOTES v0.8.0 (задеплоен). Код: `/api/chat/init` (get_chat_data) + `src/lib/auth-cache.ts` (1×getUser + 1×profiles на запрос). Деплой `73e60ff` → `deploy-1788547720` | релиз |
| #39 | Холодный старт контейнера: первый заход на `/admin/history` и `/admin/stats` на проде 20–30 сек + редкий редирект на авторизацию | **Решение (05.09.2026, бюджет-оптимум):** зависания убраны в коде (BFF `/api/lesson/*`, прокси storage с anon-key — фикс 403 mp3, sw.js, `/api/health`). Автопинг НЕ ставим: каждые 4 мин = контейнер всегда «включён» ≈ аренда (деньги, C1/C2); GitHub-пинг жжёт бесплатные минуты Actions (нужны для деплоев). Холодный старт остаётся как «первый заход 20–30с после простоя» — терпимо, 0 ₽. Ручной прогрев перед учениками: workflow `Warm container (manual)`. Настоящий аптайм — это C1/C2 (позже, при росте). Осталось: регресс-тест урока + деплой. | готово (решение и код в деплое; холодный старт — норма, прогрев вручную через `Warm container (manual)`) |
| #40 | TipTap-редактор: картинки — пропал ресайз-хендл и числовой размер в px; сохранённый масштаб «схлопывается» в редакторе до точки и его нельзя расширить (тест-курс, урок 2; у директора та же беда при редактировании уроков) | Симптомы: (1) в правом нижнем углу картинки не появляется метка-курсор для растягивания + под ней не показывается размер в пикселях (раньше было); (2) после сохранения редактора масштаб изображения часто схлопывается до маленькой точки — потом не растянуть; в предпросмотре видно не всегда, на живом сайте грузится, но медленно. Касается только редактора (учитель), не страницы урока. Разбор — в `src/components/resizable-image.tsx` / `tiptap-editor.tsx` (атрибут `style: max-width Npx`, ресайз-хендл по `selected`). **В работе (05.09.2026):** фикс в коде — `resizable-image.tsx` (proxy src + `Math.round` px), `block-renderer.tsx`, `blocks-editor.tsx` (уйдёт в один регресс с батчем стабильности). | готово (код в деплое; подтверждение — регресс на проде) |
| #41 | Тип задания «Выбор изображения» (Image Choice) не отображается в предпросмотре урока — блока задания вообще не видно | Учитель создал в уроке задание типа «Выбор изображения» — в предпросмотре урока его нет (само задание исчезло из отображения). Разбор — рендер блока-рендерера для типа ImageChoice в `src/components/lesson-blocks/` (проверить, зарегистрирован ли тип в роутере/БД при сохранении). **В работе (05.09.2026):** фикс в коде — `resizable-image.tsx` (proxy src + `Math.round` px), `block-renderer.tsx`, `blocks-editor.tsx` (уйдёт в один регресс с батчем стабильности). | готово (код в деплое; подтверждение — регресс на проде) |
| #42 | Редактор задания: нажал «Сохранить» — нет НИКАКОГО подтверждения сохранения (ни тоста, ни надписи рядом с кнопкой) — непонятно, сохранилось ли, можно ли закрывать окно редактирования и переходить к след. заданию | В редакторе блока/задания после «Сохранить» нет обратной связи вовсе. **Требование:** добавить подтверждение сохранения рядом с кнопкой (и/или тост «Сохранено», автоскрытие), чтобы было видно состояние save. **Проверить так же в аналогичных местах** (другие редакторы/формы: курс, настройки, контент-админка) — единый паттерн обратной связи после сохранения. Разбор — компоненты редакторов/сохранения (`tiptap-editor`, редакторы блоков, формы). **В работе (05.09.2026):** фикс в коде — `resizable-image.tsx` (proxy src + `Math.round` px), `block-renderer.tsx`, `blocks-editor.tsx` (уйдёт в один регресс с батчем стабильности). | готово (код в деплое; подтверждение — регресс на проде) |
| #43 | Подаренный доступ: админ подарил ученику доступ «в подарок» на 30 дней через «Доступ к курсу» — в шапке показаны НЕ дни подарка, а дни подписки («10 дней») | **Причина (05.09.2026):** в шапке считались ТОЛЬКО дни подписки (`subscription_until`), а подарок 30 дней — это другой счётчик (`course_access`, конкретный курс). **Итоговое решение** (после уточнения владельца): НЕ смешивать понятия в один «макс» — ПОДПИСКА (подписка на сайт, `subscription_until`, дни в шапке) и ДОСТУП к курсу (персональный подарок на конкретный курс, «навсегда» или «до даты») — это РАЗНЫЕ вещи. Шапка показывает только подписку; подаренные курсы видны отдельно (на `/courses` и в настройках блок «Доступ к курсам»). Исправлено в `site-header.tsx` + `settings` | проверено |
| #44 | Ученик, которому подарили доступ: тестовый курс в списке доступных есть, но клик по ЛЮБОМУ уроку возвращает обратно на список уроков курса — внутрь урока не попасть | **Разбор (05.09.2026):** «выброс» возникал в двух местах, оба теперь понятны и исправлены: (1) урок-черновик не возвращался — верно по логике; (2) ГЛАВНОЕ: RPC не отдавал `published`, из-за чего ученика выкидывало из ЛЮБОГО урока — см. #47 (исправлено, проверено входом ученика). Проверка пользователя в общем регрессе | проверено (вход ученика в урок — на живых данных) |
| #45 | Мобильная версия: ЛИШНИЕ ПРОБЕЛЫ вокруг цифр везде — видно «7 8» вместо «78», «1 0» вместо «10» и т.п. | **Причина (05.09.2026, телефон Xiaomi + Chrome):** цифры рисуются запасным шрифтом (латиница/цифры) с широкими глифами, кириллица — системным → визуальный пробел между цифрами. **Фикс в коде:** встроен единый шрифт `Inter` (латиница+кириллица из одного семейства, self-hosted через `@fontsource/inter`, 400–700) + единый стек в `globals.css`. На тестовом устройстве не проверить (только прод); проверить после деплоя на телефоне (очистка кэша). | готово (код в деплое; подтвердить на телефоне в регрессе) |
| #46 | Задание «Вставка слова» (fill_blank): в предпросмотре урока рядом с фразой появляется русский перевод + мусор(`_ ... _ …">`), а «прослушать всё предложение» при клике/правом клике по фразе не работает | **Причина найдена (05.09.2026):** (1) у переводимой фразы перевод лежит в атрибуте `<span data-translate="Завтра [[дурак]] …">`, и сам атрибут содержит маркеры `[[…]]`; рендерер FillBlank заменял ВСЕ `[[…]]` во всей строке, включая те, что внутри атрибута → в атрибут вставлялся `<input>`, HTML ломался → в предпросмотре утекали «_ ... _», русский перевод и `">`; (2) на fill_blank вообще не был подключён попап перевода/озвучки (он есть только у текстовых блоков). **Фикс в коде (`block-renderer.tsx`):** новая функция `replaceBracketsOutsideTags`/`buildFillBlank` — замена `[[…]]` только в тексте, атрибуты не трогаются; на fill_blank добавлен попап «перевод + 🔊 Прослушать» (клик/правый клик по фразе `data-translate`). Осталось проверить в регрессе. | готово (код в деплое; проверка — регресс на проде) |
| #47 | Ученик НЕ может открыть урок, даже опубликованный: раньше молча уводило на список, теперь «Урок ещё не опубликован» — но урок включён | **Причина (05.09.2026, живые данные):** RPC `get_lesson_page` НЕ возвращает поле `published` урока (`rpc-aggregation.sql`: в `SELECT` урока колонки нет) → проверка `lesson.published` всегда «ложь» → любой ученик выкидывался из ЛЮБОГО урока (для черновика и для публикации одинаково). **Фикс (`[lessonId]/page.tsx`):** опубликованность определяется по `all_lessons` (RPC кладёт туда только опубликованные). Проверено на живых данных: madhamster с подарком входит в урок 2 тест-курса, контент рендерится («не опубликован» не показывается) | проверено |
| #48 | Расхождение сроков доступа: шапка «30 дн.», настройки «До 15.09.2026» — «это как?» | **Причина (05.09.2026):** я смешал два разных счётчика в один «максимум»: подписку (`subscription_until`) и персональный доступ к курсам (`course_access`). Пользователь прав: это РАЗНЫЕ понятия. Подписка — общий доступ к сайту (срок, дни в шапке); ДОСТУП — подарок на конкретный курс (навсегда или до даты, от подписки не зависит). **Фикс:** (1) шапка ← только подписка: «N дн.» (title «Подписка активна до …»); (2) раздел «Подписка» на `/settings` ← только подписка («✓ Подписка активна / До …»), без «Доступ активен/Навсегда»; (3) добавлен ОТДЕЛЬНЫЙ блок «Доступ к курсам»: список подаренных курсов со сроком («навсегда» / «до …» / «закончился»). «Навсегда»-подарок madhamster уже в БД (`expires_at=NULL`, 2 курса) | проверено |
| #49 | Озвучка фраз: вместо мужского испанского — «женский монотонный каверкающий» | **Причина (05.09.2026):** на Windows-машине НЕТ испанских голосов (только русские «Microsoft Irina»/«Microsoft Pavel»). «Женский» голос — это русский Irina, читающий испанский фонетически. **Фикс (`speech.ts`, `speakSpanish`):** испанской озвучки не-испанским голосом больше НЕТ; ожидание `voiceschanged`, мужской приоритет; нет испанских (или только женские) → серверная `/api/tts` (Edge neural мужской → Google Translate TTS). **Дополнительно (05.09):** модалка словаря и др. места всё ещё звали `speechSynthesis` напрямую (старый голос): `vocab-picker-fab`, `add-word-from-lesson`, `tiptap-editor`, «Словарь» и «Карточки» в инструментах — все 5 переведены на `speakSpanish`. **Ещё (05.09, замечание пользователя):** повторное нажатие 🔊 порождало несколько одновременных речей («эхо») — серверная озвучка не отменяла предыдущую. Теперь в `speech.ts` одиночное воспроизведение: любое новое нажатие гасит текущее (`stopSpeaking`, токен-инвалидация, `onEnd`-колбек), а кнопки 5 мест блокируются до конца озвучки и подсвечиваются (hover + «Озвучиваю…»). Проверено: `/api/tts` → 200 audio/mpeg | проверено (пользователь подтвердил озвучку) |
| #50 | Аудио-плеер урока «не играет» (в редакторе и на уроке) | **Итоговая причина (05.09.2026):** композитный дефект. (1) SW кэшировал `/api/storage/*` cache-first с таймаутом 15с → рисковый 503; (2) блок аудио хранился как `video` + mp3-src, рендер выбирал тег по типу блока → mp3 попадал в `<video>` («нет перемотки, не играет») — фикс: тег по РАСШИРЕНИЮ; (3) **ГЛАВНОЕ:** media-элементы шлют `Accept-Encoding: identity;q=1,*;q=0`, а dev-сервер Next НЕ доставляет ТЕЛО динамического route-handler ответа для identity (заголовки 206 доходили, байты зависали) — одинаково для 200/206/302 динамических ответов; при этом СТАТИКА под `public/` отдаётся мгновенно (проверено: 7–140 мс, любой Range); (4) Next-patched fetch рвёт апстрим-Range (>5с keep-alive supabase-шлюза → `TypeError: terminated`), домашний ISP при этом капризен (ECONNRESET). **ФИНАЛЬНЫЙ ФИКС (dev, рабочее решение):** медиа-файлы материаллизуются локально в `public/_media/<basename>` (отдельный node-процесс `scripts/materialize-media.mjs`, куски 1 МБ × retry 6 — в in-process флейк) через dev-роут `/api/dev/media?path=...`; новый компонент `src/components/lesson-blocks/media-asset.tsx` в dev направляет `<audio>/<video>` на `/_media/<basename>` (onError → материаллизация → повтор), в прод — на прежний прокси. Не-картинки читаются `node:https` `rawGet`+retry (не Next fetch); dev-кап Range >1МБ. Прод-путь — динамический буферный ответ (там сети стабильные). **Проверено:** headless `<audio src="/_media/..." readyState=4, networkState=1, duration=246.4с` (раньше readyState=0/stalled); статика отдаёт любой Range за 7мс. Файл 7 886 053 Б материаллизован в `public/_media/1783177291666.mp3`. **Проверено пользователем в Chrome 05.09:** плеер играет, шкала времени есть, перемотка работает (вкл. открытие `/_media/1783177291666.mp3` в новой вкладке) | проверено |
| #51 | Прод-регрессия после deploy v0.8.1: навёл на сайт → «Войти» → `{"errorMessage":"unexpected EOF"}`, потом «чёрная страница», плеер урока — 502, картинки грузятся медленно | **Причина (05.09.2026, измерил прямыми curl):** Yandex Serverless Containers убивает ЛЮБОЙ ответ с телом > **3 670 016 Б (~3.5 МБ)** → `JobResponseTooLong` → контейнер падал посреди загрузки (EOF/чёрная страница). Storage-роут на проде отдавал mp3 (7,8 МБ) целиком даже при `Range: bytes=0-` (это «от 0 до конца») — убивался плеер урока. Картинки медленные — холодные инстансы + дорогой upstream без прогрева. **Фикс (route.ts):** не-картинки (аудио/видео/PDF) теперь в ЛЮБОМ окружении отдаются ПО СЛАЙСАМ ≤1 МБ — клиентский Range клэпится до `start..start+1МБ`, plain GET → первый слайс как 206 с `Content-Range`; плеер дочитывает дальше маленькими запросами (как статик-сервер). Ошибок >3.5МБ больше нет. Осталось: проверка на проде после деплоя (плеер mp3, EOF, «чёрная страница») | в работе |
| #52 | Редактор урока: «Сохранить» висит 10–20с в режиме «Сохраняю…» даже без изменений | **Причина (05.09.2026):** после успешного апсерта в Supabase (0.2с) вызывался `router.refresh()` — SSR-перерисовка ВСЕЙ страницы урока: на проде холодный контейнер + RSC-ответ мог превышать лимит 3.5МБ (#51, из-за огромных srcset со signed-URL) → refresh не завершался. **Фикс (blocks-editor.tsx):** убраны `router.refresh()` из save/delete/reorder (локальный стейт уже обновлён, на сервер пишется) — кнопка отрабатывает мгновенно с тостом «✓ Сохранено». Осталось: проверка на проде после деплоя | в работе |

### 2026-09-04 (сессия) — v0.8.0 подготовлен: чат на 1 запрос + быстрый дашборд
- **Деплой 1:** чат через `POST /api/chat/init` → RPC `get_chat_data` (было 5–7 запросов). Fallback `legacyInit`, чат не ломается.
- **Деплой 2:** `src/lib/auth-cache.ts` — `getServerClient`/`getCurrentUser` через `React.cache()`; все серверные дашборд-страницы переведены (было 3×`getUser` + 2×`profiles`, стало 1+1 на запрос).
- **B7 отклонён:** `revalidate` не даёт ISR (кастомный `supabaseFetch` без `next:{revalidate}`), риск устаревания контента учителя.
- Проверка: `tsc --noEmit` чисто, `npm run build` успешен. Браузерное регресс-тестирование (вход, курсы, урок, чат, настройки, учительская) — 0 ошибок.
- **Деплой: `73e60ff` → ревизия `deploy-1788547720` (ACTIVE)** — v0.8.0 на проде, `/api/chat/init` и `/api/stats` отвечают 401 без авторизации (корректно).
- **Гигиена:** YCR очищен до 2 тегов (`deploy-1788548226`, `deploy-1788547720`). Новая ишью #39 (холодный старт на проде, низкий приоритет).

---
- `de0178d` → ревизия `deploy-1788544153` (ACTIVE):
- **Доступ к неопубликованным урокам** — админ/владелец открывают черновик; список уроков скрывает черновики от учеников (`page.tsx` + RPC `get_course_page`).
- **«Очистить ответы»** — 1 RPC `clear_lesson_answers` (одна транзакция) + `router.refresh()` вместо 2 запросов + reload (было ~1 мин, стало мгновенно).
- **Аватары** — единый компонент `<Avatar>` (прокси `?w=36&fm=webp`, fallback-буква) во всех местах (шапка/настройки/чат/уведомления/учительская).
- **Кэш картинок** — in-memory кэш в прокси `/api/storage` (10 мин, ~500 записей, `X-Storage-Cache`).
- **Статистика** — `/api/stats` переведён на `no-store` (устранён «застрявший» счётчик пользователей).
- **Удалены 16 тестовых учёток** (13 `@test.ru` + 3 безымянных); осталось 5 реальных.

---

## История (закрытые)

_(крупные вехи и закрытые ишью)_

### 2026-09-03 — v0.7.0 (релиз) + v0.7.1 (Hero hotfix): мобильная оптимизация
- `fd77f6c` — perf: прокси `/api/storage` (`?w/?q/?fm` + Accept-negotiation + `Vary: Accept` + Cache-Control + `sharp.rotate()`), custom image loader + `StorageImage`, `next.config.ts` (custom loader + formats)
- `c134ce9` — миграция публичных страниц на `StorageImage` (главная, catalog, content/[id], reviews, about, teachers, teachers/[id], content-carousel, слайдшоу, CTA). Прокси умеет signed URL, `upload-file` → public URL, TwemojiGlobal — skip обработанных поддеревьев
- `e530616`, `02478ae` — деплой v0.7.0 (`deploy-1788436255`, прод 200/быстрый/gzip через curl)
- `v0.7.1` — Hero hotfix: слайды WebP (`?w=1280&q=65&fm=webp`) + предзагрузка первых слайдов (Hero до ~60с → почти мгновенно)

### 2026-07-19 — v0.5.0: divider + alignment + owner bypass + docs
- `d47463b` — fix: divider React NodeView, image alignment CSS fallback. Файлы: `tiptap-divider.tsx`, `resizable-image.tsx`, `block-renderer.tsx`, `tiptap-editor.tsx`
- `98c84a5` — fix: teacher/owner bypass для course access check
- `2912403` — fix: lesson blocks rendering, divider visibility
- `df65c8f` — v0.5.0: per-course access system + docs

### 2026-07-12 — deploy-1783879569 (стабильный релиз)
- Стабильная версия до v0.5.0

### 2026-07-07 — v0.4.3: proxy images, server auth, storage proxy
- `485aa98`

### 2026-07-06 — v0.4.2: reorder fix, features seed, CTA Infinity
- `6981d6b`

### 2026-07-04 — v0.4.1: image optimization, Docker Sharp
- `b201f09`, `0ab747f`

### 2026-07-03 — CI/CD pipeline stable
- 17 коммитов отладки GitHub Actions → Yandex Cloud деплой

### 2026-07-02 — deploy-016: lucide icons, Sharp, 4 cards, docs
- `26e409f`

### 2026-07-01 — deploy-015: first stable Yandex Cloud deploy
- Container: `bbaiqrnaqkaglikupvdm`, env vars fixed
- GitHub Actions настроены, YC_SA_KEY_JSON в secrets

### 2026-06-29 — MVP launch (portal + lessons + translations)
- `264bbd6`

---

## Инфраструктура

| Компонент | ID / URL | Статус |
|---|---|---|
| Container | `bba12ti21lgmv9glfl7k` — `naranja-backend` | ✅ ACTIVE |
| Gateway (новый) | `d5dgv9o24fhti6ds7hde` | ✅ ACTIVE |
| Домен | `naranja.outmilk.online` | ✅ привязан, сертификат ISSUED |
| Сертификат | `fpqc35fc9ueg8u78gacv` (Let's Encrypt) | ✅ до 2026-10-06 |
| Registry | `crpusm23v7g9ch5c5t9h` | ✅ |
| SA контейнера | `ajep2inmg605fd6ttbb2` | ✅ editor |
| SA GitHub Actions | `ajefscetirf01br3unuc` | ✅ pusher + editor + viewer |
| Supabase | `https://zphehhzgbudetyzezunk.supabase.co` | ✅ |
| Репозиторий | `https://github.com/outmilker1978/-naranja-feliz.git` | ✅ |



| #51 | Глобальная интерактивность: многие кликабельные элементы (значки, кнопки, ссылки) не дают явной реакции — как понять, что элемент кликабельный (цвет/тень при наведении, hover/active) | Пользователь: «все кнопки/иконки/тексты как-то интерактивно подсвечивались — цвет, тень, что угодно». Сделано точечно для кнопок озвучки (5 мест, hover + busy). Нужен системный проход по всем UI: добавить hover/active/focus states, cursor-pointer единообразно (паттерн в globals.css, чек главного каркаса: шапка, карточки курсов/уроков, инструменты, чат, уведомления) | бэклог |

---
## СЕССИЯ 05.09.2026 (вечер) — закрытие

### Статусы по текущему батчу
| № | суть | статус |
|---|---|---|
| #49 .. #52 (озвучка, аудио-блок, большой откат EOF, refresh) | закрытый батч релиза v0.8.1 | **проверено на проде** |
| #51-big | лимит 3.5MB Yandex Serverless (`JobResponseTooLong`): большой откат EOF / бинарные файлы | **проверено на проде** (см. ниже «Как отдаются файлы») |
| #52 | «Сохранить урок» висел 10-20с из-за RSC/3.5MB | **проверено на проде** |

### Как сейчас отдаются файлы ≥3.5MB (прод, активен deploy-1788622600)
- Картинки: BFF-прокси resize (`/api/storage/...`) — не менялось.
- Не-картинки (аудио/видео/PDF): BFF сам шлёт слайсы `bytes=0-1M` (Range-125) и клиент склеивает через `fix-range`/сборщик. Медиа — фрагменты 206, PDF — веб-страница-сборщик (открывается просмотрщиком без ошибок, проверено end-to-end).
- **Подписанные URL (307 на Supabase) — написан код, на проде НЕ активен:** `createSignedUrl` внутри контейнера падает (внешний REST-тест с `apikey`+anon проходит и выдаёт `signedURL`). Нужна отладка в контейнере (отдельная ишью).

### Новые ишью
| № | суть | статус |
|---|---|---|
| #53 | Отдача файлов через 307-подписанный URL Supabase (обход 3.5MB, цельный файл вместо сборки). Код написан (user-scoped `createClient`), падает в рантайме в контейнере — нужна трассировка `createSignedUrl`/supabaseFetch (Connection:close, timeout 12s). | в работе (след. сессия) |
| #54 | «Вставить файл по ссылке» в редакторе урока (сейчас PDF/Aудио — только загрузка с ПК; аудио/видео-блоки имеют поле «Ссылка», у текстового редактора — нет). Добавить инструмент: вставка по URL + заголовок → `<a target="_blank">`. | бэклог |
| #55 | `SUPABASE_SERVICE_ROLE_KEY` в `.env.local` **просрочен** (`Invalid Compact JWS` на `/storage/v1/object/sign` даже с `apikey`). Ломает `createServiceClient/createAdminClient`: оплата (ЮKassa → Supabase), админ-действия. Замена: Supabase Studio → Settings → API → service_role → обновить `.env.local` + GH secrets + env ревизии. Ученик-путь (вход/логин/уроки) не затронут. | бэклог (нужны действия пользователя) |
| #56 | Мусор в YCR: удалить теги `deploy-1788620200/1170/1750/2200` при закрытии (оставить `deploy-1788622600` + предыдущий). | бэклог (гигиена) |

### Наблюдения
- Ранний вывод «service-role сломан» из-за отсутствия `apikey`-заголовка оказался частично ложным: для `sign`-эндпоинта нужен ОБА заголовка `apikey` + `Authorization`. Повторная проверка с `apikey` подтвердила: **anon-ключ валиден, service-role действительно просрочен (#55)**.
